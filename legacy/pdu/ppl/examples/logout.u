#include "/usr/include/stdio.h"
#include "PDUppl_hdr.h"

extern PDUppl_logout();
extern _pdm_debug();

main()
{

int	return_status;

pdm$logout(    status	= &return_status,
          );

if (return_status != PDM_S_SUCCESS)
  _pdm_debug("API logout failed", 0);
}
