#include	"PDUppl_hdr.h"


main()
{
  int status;

  pdm$activate_part(status = &status, catalog_name = "standard_cat",
                    part_id = "macropart1", revision = "a", 
                    file_name = "macro_add1a");
exit;  
}
