if (test $# = 0)
	then
	echo "Need a time argument"
	exit
	fi

for arg in 1
do
    cd /usr6/ip32/modeldp/bin
	chmod 644 Model

    cd ../
	find \
	bin/Model \
	-print | cpio -ovc | compress | \
	fmu pdmdel.root.newroot send - /usr5/ws_s/i_modeldbg/model.p

done
