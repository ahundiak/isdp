if (test $# = 0)
	then
	echo "Need a time argument"
	exit
	fi

for arg in 1
do
    cd ./pdusolsrc
        ftp -n ${DELNODE} <<- END
                user root ${DEL_PSWD}
                binary
                put install.ssl ${WS_DIR}/i_pdusolsrc${VERREF}/install.ssl
                put remove.ssl ${WS_DIR}/i_pdusolsrc${VERREF}/remove.ssl
                put share.ssl ${WS_DIR}/i_pdusolsrc${VERREF}/share.ssl
                put product.def ${WS_DIR}/i_pdusolsrc${VERREF}/product.def
                put README ${WS_DIR}/i_pdusolsrc${VERREF}/README
                quit
		END
    cd ..
    dates > product.dates
done
