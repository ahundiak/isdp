if (test $# = 0)
	then
	echo "Need a time argument"
	exit
	fi

for arg in 1
do
    cd ../
	chmod 755 config/initial
	chmod 644 config/initial/*

	touch $1 config/*
	touch $1 config/initial/*

	find \
	config/initial/* \
	-print | sort | cpio -ovc | compress > pdmconf.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmconf.ssl /usr3/ws_s/i_pdmsolsrc/pdmconf.ssl
                quit
		END
        rm pdmconf.ssl
        echo

	find dbasupport \
		-print | sort | cpio -ovc | compress > pdmdbaspt.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmdbaspt.ssl /usr3/ws_s/i_pdmsolsrc/pdmdbaspt.ssl
                quit
		END
        rm pdmdbaspt.ssl
        echo

	find ass \
		-print | sort | cpio -ovc | compress > pdmass.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmass.ssl /usr3/ws_s/i_pdmsolsrc/pdmass.ssl
                quit
		END
        rm pdmass.ssl
        echo

	find bom \
		-print | sort | cpio -ovc | compress > pdmbom.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmbom.ssl /usr3/ws_s/i_pdmsolsrc/pdmbom.ssl
                quit
		END
        rm pdmbom.ssl
        echo

	find cicocp \
		-print | sort | cpio -ovc | compress > pdmcicocp.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmcicocp.ssl /usr3/ws_s/i_pdmsolsrc/pdmcicocp.ssl
                quit
		END
        rm pdmcicocp.ssl
        echo

	find fmgr \
		-print | sort | cpio -ovc | compress > pdmfmgr.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmfmgr.ssl /usr3/ws_s/i_pdmsolsrc/pdmfmgr.ssl
                quit
		END
        rm pdmfmgr.ssl
        echo

	find init \
		-print | sort | cpio -ovc | compress > pdminit.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdminit.ssl /usr3/ws_s/i_pdmsolsrc/pdminit.ssl
                quit
		END
        rm pdminit.ssl
        echo

	find make \
		-print | sort | cpio -ovc | compress > pdmmake.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmmake.ssl /usr3/ws_s/i_pdmsolsrc/pdmmake.ssl
                quit
		END
        rm pdmmake.ssl
        echo

	find include \
		-print | sort | cpio -ovc | compress > pdminclude.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdminclude.ssl /usr3/ws_s/i_pdmsolsrc/pdminclude.ssl
                quit
		END
        rm pdminclude.ssl
        echo

	find lib/Sol2 \
		-print | sort | cpio -ovc | compress > pdmlib.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmlib.ssl /usr3/ws_s/i_pdmsolsrc/pdmlib.ssl
                quit
		END
        rm pdmlib.ssl
        echo

	find macros \
		-print | sort | cpio -ovc | compress > pdmmacros.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmmacros.ssl /usr3/ws_s/i_pdmsolsrc/pdmmacros.ssl
                quit
		END
        rm pdmmacros.ssl
        echo

	find pdmapi \
		-print | sort | cpio -ovc | compress > pdmapi.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmapi.ssl /usr3/ws_s/i_pdmsolsrc/pdmapi.ssl
                quit
		END
        rm pdmapi.ssl
        echo

	find mulfile \
		-print | sort | cpio -ovc | compress > pdmmulfile.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmmulfile.ssl /usr3/ws_s/i_pdmsolsrc/pdmmulfile.ssl
                quit
		END
        rm pdmmulfile.ssl
        echo

	find pdmp \
		-print | sort | cpio -ovc | compress > pdmpdmp.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmpdmp.ssl /usr3/ws_s/i_pdmsolsrc/pdmpdmp.ssl
                quit
		END
        rm pdmpdmp.ssl
        echo

	find pdms \
		-print | sort | cpio -ovc | compress > pdmpdms.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmpdms.ssl /usr3/ws_s/i_pdmsolsrc/pdmpdms.ssl
                quit
		END
        rm pdmpdms.ssl
        echo

	find reports \
		-print | sort | cpio -ovc | compress > pdmreports.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmreports.ssl /usr3/ws_s/i_pdmsolsrc/pdmreports.ssl
                quit
		END
        rm pdmreports.ssl
        echo

	find ris \
		-print | sort | cpio -ovc | compress > pdmris.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmris.ssl /usr3/ws_s/i_pdmsolsrc/pdmris.ssl
                quit
		END
        rm pdmris.ssl
        echo

	find tools \
		-print | sort | cpio -ovc | compress > pdmtools.ssl
	ftp -n pdmdel <<- END
                user root mdel
                binary
                put pdmtools.ssl /usr3/ws_s/i_pdmsolsrc/pdmtools.ssl
                quit
		END
        rm pdmtools.ssl
        echo

done
