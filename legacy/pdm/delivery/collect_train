if (test $# = 0) 
	then
	echo "Need a time argument"
	exit
	fi

for arg in 1
do
        cd ..
	find training -print | sort | cpio -ov | compress | \
	fmu pdmdel.root.rtpdel send - /usr3/ws_s/i_pdm$VERREF/pdmtrain.p
done
