# Copyright Intergraph Corporation	1991
# Install FONTSERVE product

NULL=/dev/null

mkdir	/usr/include /usr/include/X11 >$NULL 2>&1

getfile fontserve.sun | compress -d | cpio -imud >/dev/null 2>&1 || exit $?

for i in `ls *.h`
do
	ln -s $4/$i /usr/include/X11 >$NULL 2>&1
done

exit 0
