# Get the product name from product.def file
. ../src/afiles/product.def
IDversion=`ksh $GRA_BIN/getVersion`
IDdate=`ksh $GRA_BIN/getDate`
export IDname IDversion IDdate

# archive the files under the directories passed to the archive script
cd $FONTSERVER
ksh $GRA_BIN/archive del src other

