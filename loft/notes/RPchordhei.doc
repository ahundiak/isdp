

		INTERGRAPH	INTERGRAPH	INTERGRAPH	INTERGRAPH






		LLL            OO        FFFFFFFFF   TTTTTTTTTTTTT
		LLL          OO  OO      FFFFFFFFF   TTTTTTTTTTTTT
		LLL         OOO  OOO     FFF              TTT
		LLL        OOOO  OOOO    FFFFFF           TTT
		LLL        OOOO  OOOO    FFFFFF           TTT
		LLL         OOO  OOO     FFF              TTT
		LLLLLLLL     OO  OO      FFF              TTT
		LLLLLLLL       OO        FFF              TTT

		__________________________________________________
		__________________________________________________






			######################################
			#                                    #
			# TEMPLATE CHORD-HEIGHTS REPORT (TC) #
			#                                    #
			#	 DOCUMENTATION               #
			#                                    #
			######################################





                Summary
                =======

                I   - Template Chord-Heights Report Over View

		II  - State Keywords
			vl-rp-tc-reg-Start
			vl-rp-tc-reg-Report
			vl-rp-tc-reg-End
			vl-rp-tc-spi-Title
			vl-rp-tc-spi-Report
			vl-rp-tc-spi-End

		III - Function Keywords
			vl-rp-tc-reg-Name()
			vl-rp-tc-reg-ChordHeight()
			vl-rp-tc-reg-Height()
			vl-rp-tc-reg-Angle()
			vl-rp-tc-reg-LeanDirection()
			vl-rp-tc-spi-Name()
			vl-rp-tc-spi-Frame()
			vl-rp-tc-spi-ChordHeight()
			vl-rp-tc-spi-Height()




I - TEMPLATE CHORD-HEIGHTS REPORT OVER VIEW
===========================================

	After locating one or several "TemplateMgr_##" occurences of
	I_LOFT "VLtplMgr" object and determining the output ASCII
	file name, the report runs.

	Reading "RPsetup" file (locally or in $LOFT/config/drawing)
	the I_STRUCT Apfel-Strudel executes orders given in it.




II - STATE KEYWORDS
===================

	Regular template related states are prefixed by "vl-rp-tc-reg".
	Spine template related states are prefixed by "vl-rp-tc-spi".

o vl-rp-tc-reg-Start :
	This first block is processed for each template manager
	"TemplateMgr_##" located, to regular template reporting.
	The regular template header is printed.

o vl-rp-tc-reg-Report :
	This block is processed for each regular template "TemplateRib_nn".
	The regular template output values are printed.

o vl-rp-tc-reg-End :
	This last block is processed for each template manager
	"TemplateMgr_##" located, to regular template reporting.
	The regular template trailer is printed.

o vl-rp-tc-spi-Title :
	This block is processed for each template manager "TemplateMgr_##"
	located.
	The spine template header is printed.

o vl-rp-tc-spi-Report :
	This block is processed for each spine template "TemplateSpine".
	The spine template output values are printed.

o vl-rp-tc-spi-End :
	This last block is processed for each template manager
	"TemplateMgr_##" located, to spine templates reporting.
	The spine template trailer is printed.




III - FUNCTION KEYWORDS
=======================

	All these functions return values that will be printed out by
        the function vl-Print().

	General functions are prefixed by "vl-rp-tc".
	Regular template related functions are prefixed by "vl-rp-tc-reg".
	Spine template related functions are prefixed by "vl-rp-tc-spi".

o vl-rp-tc-reg-Name() :
	Returns the name of the regular template processed.

	synopsys :
	$nam := vl-rp-tc-reg-Name();
		$nam is a string.

o vl-rp-tc-reg-ChordHeight() :
	Returns the chord-height of the regular template processed.

	synopsys :
	$hei := vl-rp-tc-reg-ChordHeight();
		$hei is a double.

o vl-rp-tc-reg-Height() :
	Returns the total height of the regular template processed.

	synopsys :
	$hei := vl-rp-tc-reg-Height();
		$hei is a double.

o vl-rp-tc-reg-Angle() :
	Returns the inclinaison angle of the regular template processed.

	synopsys :
	$ang := vl-rp-tc-reg-Angle();
		$ang is a double.

o vl-rp-tc-reg-LeanDirection() :
	Returns the leaning direction of the regular template processed.

	synopsys :
	$nam := vl-rp-tc-reg-LeanDirection();
		$nam is a string.

	eligible values :
		FWD	forward (+X)
		AFT	aft (-X)
		PRT	portside (+Y)
		STB	starboard (-Y)
		TOP	top (+Z)
		BOT	bottom (-Z)

o vl-rp-tc-spi-Name() :
	Returns the name of the spine template processed.

	synopsys :
	$nam := vl-rp-tc-spi-Name();
		$nam is a string.

o vl-rp-tc-spi-Frame() :
	Returns the name of the frame intersecting the spine template.

	synopsys :
	$nam := vl-rp-tc-spi-Frame();
		$nam is a string.

o vl-rp-tc-spi-ChordHeight() :
	Returns the chord-height of the spine template processed.

	synopsys :
	$hei := vl-rp-tc-spi-ChordHeight();
		$hei is a double.

o vl-rp-tc-spi-Height() :
	Returns the total height of the spine template processed.

	synopsys :
	$hei := vl-rp-tc-spi-Height();
		$hei is a double.
