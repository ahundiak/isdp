#include <stdio.h>
#include <FI.h>

extern int GRtext_param_init();

main(argc, argv)
int argc;
char *argv[];
{
    int sts;
    char *form_name;

    // check command line args
    if (argc != 2)
        return;

    // kick off the form, if it is supported
    form_name = argv[1];
    if (strcmp(form_name, "GRTextParam") == 0)
        GRtext_param_init(form_name);
}
