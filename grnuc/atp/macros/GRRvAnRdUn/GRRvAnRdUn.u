//~Name      : 
//~Originator: dodd
//~Originated: (RAP) Fri Feb  2 14:29:10 1990
//~Product   : I/GRNUC 01.03.00.70 Thu Feb  1 15:15:15 CST 1990
//~Nodename  : koz
//~Command   : 
//~Comments  : 

#include "ciminimum.h"
#include "cimacros.h"
#include "fi.h"

main()
{
	double pt[3];
	struct FI_data_st formData;


	ci$put(string = "rev ang read");
	//"GRRvAnRdUn" "review angular readout units"

	formData.label = 55;
	formData.type = FI_STATE;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 55;
	formData.type = FI_STATE;
	formData.index = 1;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 1;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 41;
	formData.type = FI_STATE;
	formData.index = 1;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 1;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 41;
	formData.type = FI_STATE;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 40;
	formData.type = FI_STATE;
	formData.index = 1;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 1;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 40;
	formData.type = FI_STATE;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 32;
	formData.type = FI_ALPHA;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	strcpy(formData.value.alpha," ");
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 31;
	formData.type = 10;
	formData.index = 1;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 31;
	formData.type = 10;
	formData.index = 2;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 31;
	formData.type = 10;
	formData.index = 0;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 30;
	formData.type = 10;
	formData.index = 1;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 30;
	formData.type = 10;
	formData.index = 2;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 30;
	formData.type = 10;
	formData.index = 0;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 39;
	formData.type = 10;
	formData.index = 1;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 39;
	formData.type = 10;
	formData.index = 2;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 39;
	formData.type = 10;
	formData.index = 0;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 33;
	formData.type = FI_ALPHA;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	strcpy(formData.value.alpha,"2");
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 34;
	formData.type = FI_ALPHA;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	strcpy(formData.value.alpha,"1");
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 35;
	formData.type = FI_ALPHA;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	strcpy(formData.value.alpha,"4");
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 49;
	formData.type = FI_ALPHA;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	strcpy(formData.value.alpha,"16");
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 36;
	formData.type = FI_STATE;
	formData.index = 1;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 1;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 36;
	formData.type = FI_STATE;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 36;
	formData.type = FI_STATE;
	formData.index = 1;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 1;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 36;
	formData.type = FI_STATE;
	formData.index = 0;
	formData.max_index = 1;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 54;
	formData.type = 10;
	formData.index = 1;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 54;
	formData.type = 10;
	formData.index = 2;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 54;
	formData.type = 10;
	formData.index = 0;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 55;
	formData.type = FI_STATE;
	formData.index = 0;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 0;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 55;
	formData.type = FI_STATE;
	formData.index = 1;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 1;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 5;
	formData.type = FI_STATE;
	formData.index = 1;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 1;
	ci$put (form = "AngReadout",
		gadget = &formData);

	formData.label = 1;
	formData.type = FI_STATE;
	formData.index = 1;
	formData.max_index = 0;
	formData.select_flag = FI_EDIT_NOSELECT;
	formData.sts = 0;
	formData.value.state = 1;
	ci$put (form = "AngReadout",
		gadget = &formData);

  }
                            
