/* 29 Jul 2010 ah
 * These are used in src/raster but I don't think the stuff actually gets called?
 * And EXgc at least should be an array
 */
#include <stdio.h>
int EXgc()       { printf("EXgc() is stubbed out \n" ); }
int EX_scr()     { printf("EX_scr() is stubbed out \n" ); }
int EXcolormap() { printf("EXcolormap() is stubbed out \n" ); }
int EX_win()     { printf("EX_win() is stubbed out \n" ); }
