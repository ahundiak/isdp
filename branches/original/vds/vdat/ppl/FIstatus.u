#include "VDppl.h"

extern FI_get_isdp_status();

main()
{
  int i1,i2,i3,i4,i5,i6;
  char buf[256];
  
  FI_get_isdp_status(&i1,&i2,&i3,&i4,&i5,&i6);
  
  sprintf(buf,"Windows %d/%d/%d, Forms %d/%d/%d", i2,i3,i1, i5,i6,i4);
  UI_status(buf);
  return 1;
}
