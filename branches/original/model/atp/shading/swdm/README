COMMAND
  Set Window Display Mode

VERSION
  2.0.1.0

TEST PROCEDURE 

  1. iso window: wf -> sh shading -> rhl -> "toggle" to wf 
     Snapshots of window at each stage (db & si)

  2. wf -> const shading for all windows using "*" 
     Snapshots of all windows (db & si)
     const shading -> frhl for all windows using <move-on> 
     Snapshots of all windows (db & si)

SNAPSHOT

HSgragad	shading style of window 	shading_style
HSdpb2		shading style in form		shading_flags.LAST_SHADING_STYLE	
save image

