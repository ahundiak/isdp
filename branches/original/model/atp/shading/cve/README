COMMAND
  Create Visible Edges

VERSION
  2.0.1.0

TEST PROCEDURE 
     
  1. Hi-res CVE on top window
     Snapshot (db & si)

  2. Smooth-shade iso window
     Med-res CVE on iso window
     Update window
     Snapshot (db & si)
     Toggle hidden edges display off
     Update window
     Snapshot (db & si)
   
  3. Shade front window
     Low-res CVE on front window
     Update window
     Snapshot (db & si)

SNAPSHOT

VEdpb	tracing toggle 			flags.VE_VHL_NO_TRACE
	tracing tolerance 		trace_tol
	gapping toggle 			flags.VE_VHL_GAPPING_LINES
	gapping distance 	 	gapping_distance	
	batch toggle			flags.VE_VHL_BATCH_MODE
  	results file name 		results_file	
	server name 			server	   	(not implemented)	
	user name 			user_name  	(n.i.)
	password 			password   	(n.i.)
	start time 			start_time 	(n.i.)

HSdpb2	vhl resolution			vhl_flags.HS_VHL_RESOLUTION
					vhl_flags.HS_VHL_LOW_RES
					vhl_flags.HS_VHL_MEDIUM_RES
					vhl_flags.HS_VHL_HIGH_RES
	results dimension		vhl_flags.HS_VHL_RESULTS_DIM
	visible layer 			vhl_visible_layer 	(n.i.)
	hidden layer 			vhl_hidden_layer  	(n.i.)
	text layer			vhl_text_layer    	(n.i.)
	clip to fence toggle 		vhl_flags.HS_VHL_CLIP_TO_FENCE
	reference file display toggle 	vhl_flags.HS_VHL_REF_DISPLAY_OFF
	copy text to layer toggle 	vhl_flags.HS_VHL_TEXT_COPY
	curve processing toggle         vhl_flags.HS_VHL_PROCESS_WIREFRAME
		
GRdpb  	visible symbology		visible_edge_sym
	hidden symbology		hesym
	display toggle			bflags.DPB_HIDDEN_SO_ON 

save image

