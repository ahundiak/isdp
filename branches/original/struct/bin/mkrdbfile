if EXNUC=`echopath EXNUC` && STRUCT=`echopath STRUCT` ; then
	${STRUCT}/bin/Mkrdbfile ${EXNUC}
	rc=$?
else
	echo "Check product_data file: either I/EXNUC or I/STRUCT are missing."
	rc=1
fi

exit ${rc}
