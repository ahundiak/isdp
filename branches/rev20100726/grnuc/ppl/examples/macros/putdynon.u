/************************************************************************/
/*									*/
/*	Program: putdynon.u						*/
/*									*/
/*	Macro: gr$put_dynamics_on()					*/
/*									*/
/*	Defined in: grdpbmacros.h					*/
/*									*/
/*	Comments:							*/
/*									*/
/************************************************************************/

#include	"ciminimum.h"
#include	"cimacros.h"
#include	"grdpbdef.h"			/* needed */
#include	"grdpbmacros.h"			/* needed */
#include	"OMerrordef.h"			/* extra */
#include	"msdef.h"			/* extra */

extern	int	GRdpb_get();
extern	int	GRdpb_put();

main()
{
  IGRlong	ms, siz, nret;
  IGRboolean	buf;

  siz = sizeof(buf);

  gr$get_dynamics_on(
	msg	= &ms,
	sizbuf	= &siz,
	buffer	= &buf,
	nret	= &nret
		   );

  if (ms == MSSUCC)
	if ( buf == TRUE)
		write("** Flag is ON ** \n");
	else
		write("** Flag is OFF ** \n");
  else
 	write("** gr$get_dynamics_all_windows failure **\n");

  // buf = TRUE;	// ON 
  buf = FALSE;		// OFF 

  gr$put_dynamics_on(
	msg	= &ms,
	sizbuf	= &siz,
	buffer	= &buf
		   );
  if (ms == MSSUCC)
	if ( buf == TRUE)
		write("** Flag is ON ** \n");
	else
		write("** Flag is OFF ** \n");
  else
 	write("** gr$get_dynamics_all_windows failure **\n");

}	/* End Main */
