#include "VDppl.h"

extern RISget_default_schema_name();

main()
{
  char buf[128];
  char name[128];

  *name = 0;
  RISget_default_schema_name(name);
  sprintf(buf,"Schema '%s'",name);
  UI_status(buf);
}
