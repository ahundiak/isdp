#include "VDppl.h"

extern VDatGetSetNameForType();
extern VDatTestStdPcmkKeys();

// Unit, Test Unit, Tree, Plates
main()
{
  IGRchar unit[32];

  VDatGetSetNameForType("StagingTree",unit);

  if (*unit == 0) {    
    ci$get(prompt="Enter Unit (XXXX)", string = unit);
  }

  VDatTestStdPcmkKeys(unit);
    
}
