COMMAND
  Set Window Shape

VERSION
  2.0.1.0

TEST PROCEDURE
  
  Set shape of top window (select by keyin) to "581, 100, x.xx"
  Snapshot (db & si)
  Sm-shade iso window
  Set shape of iso window (select by data-point) to "A-portrait"
  Snapshot (db & si)

SNAPSHOT

  IGEgragad 	 width		min_x_dep, max_x_dep	
		 height		min_y_dep, max_y_dep

  save image
