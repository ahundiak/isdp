#include "VDppl.h"

extern VDatLoadPcmks();
extern VDatLoadPcmksFromXmlFile();

extern VDatGetSetNameForType();

// Unit, Test Unit, Tree, Plates
main()
{
  IGRchar unit[32];

  VDatGetSetNameForType("StagingTree",unit);
  //VDatLoadPcmks(unit);  
  VDatLoadPcmksFromXmlFile(unit);  
}
