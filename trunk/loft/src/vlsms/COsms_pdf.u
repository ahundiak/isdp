/* ------------------------------------------------------
 * Converts SmsData to spades
 */

#include "VDppl.h"

extern VDatGetSetNameForType();
extern VLsms2GenerateDxfPdf();

main()
{
  IGRchar name[32];
  
  VDatGetSetNameForType("StagingTree",name);

  if (*name == 0) goto wrapup;
  
  VLsms2GenerateDxfPdf("SmsData.xml",name,name,0);

 wrapup:
  return 1;
  
}
