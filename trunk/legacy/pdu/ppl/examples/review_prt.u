#include "PDUppl_hdr.h"

main()
{

int	status;
struct PDUattribute_info *attr_list;
struct file_info *file_list;

attr_list = NULL;
file_list = NULL;

pdm$review_pt(
		status = &status,
		catalog = "file_test",
		part_id = "ris_test4",
		part_rev = "a",
		attrs    = &attr_list,
		files    = &file_list );
}
