#include	"PDUppl_hdr.h"


main()
{
  int status;

  pdm$where_used(status = &status,
		catalog_name = "topdown_cat", 
              	part_id = "part21",
		revision = "a",
		report_name = "where_high",
                filename = "where_high",
                display = 1);
}
