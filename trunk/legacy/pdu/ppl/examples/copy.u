#include "PDUppl_hdr.h"

#define FALSE 0
#define TRUE  1

extern PDUppl_copy_files();
extern PDUfill_in_string();
extern _pdm_debug();

main()
{

int	return_status;

pdm$copy_files(		status 		= &return_status,
			catalog_name	= "standard_cat",
			partid		= "macropart1",
			revision	= "a",
			activate_flag	= 0
		 );

if (return_status != PDM_S_SUCCESS)
  _pdm_debug("API copy files failed", 0);
}
