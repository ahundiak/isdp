#include "PDUppl_hdr.h"

extern PDUppl_checkin_part();
extern _pdm_debug();

main()
{

int	return_status;

pdm$checkin_part(	status 		= &return_status,
			catalog_name	= "standard_cat",
			partid		= "macropart1",
			revision	= "a",
 		);

if (return_status != PDM_S_SUCCESS)
  _pdm_debug("API checkin part failed", 0);
}
