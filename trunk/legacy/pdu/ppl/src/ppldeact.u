#include "ciminimum.h"
#include "cimacros.h"

extern _pdm_debug();
main()
{
 int     status;

 _pdm_debug("In the function main in the file ppldeact.u", 0);

    ci$put(cmd = "deactivate part");
    ci$put(string = "y");
  exit;

}
