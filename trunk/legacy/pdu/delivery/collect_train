if (test $# = 0)
	then
	echo "Need a time argument"
	exit
	fi

for arg in 1
do

	cd $PDU
	find training \
	-print | sort | cpio -ovc | compress | \
	fmu pdmdel.root.rtpdel send - /usr3/ws_s/i_pdu${VERREF}/pdutrain.p

done
