#include "NETdef.h"

/* The #defines contained in this file (2.3.0.?) where being duplicated
   in NETdef.h,  NFMlisten.h has now been modified to include NETdef.h and
   the duplicate entries have been removed from NFMlisten.h B.W. 10-25-91
   NFMlisten.h can be phased out in the future */
