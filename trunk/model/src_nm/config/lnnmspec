if [ $# = 0 ]     
 then
   echo 'Usage: lnnmspec <product path>'
   echo 'Example: lnnmspec $MODEL'
   exit
fi

	echo Linking .S files to $1/spec directory
	ln ${NM}/spec/*		$1/spec/. 2>/dev/null

	echo Linking .h files to $1/include directory
	ln ${NM}/include/*	$1/include/.

	echo Done
