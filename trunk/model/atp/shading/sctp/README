COMMAND
  Set Color Table Parameters 

VERSION
  2.0.1.0

TEST PROCEDURE 
     
  Sm-shade iso window
  Snapshot (dither mode "1/2 Tone", calibrate colors "Yes")
  Dither mode "1/2 Tone", calibrate colors "No"
  Snapshot
  Dither mode "Direct", calibrate colors "No"
  Snapshot
  Dither mode "Direct", calibrate colors "Yes"
  Snapshot
  Color table display "ON"		
  Snapshot

SNAPSHOT

IGEcolor	colors			num_colors = colors * shades
		shades 			
					colors
	
HSctbl2		calc_mode		user_colors 	
					user_shades
					device_colors
					device_shades

HSdpb2		disp_mode		shading_flags.HS_SHADING_TABLE_DISP
					(not implemented)
		dither_mode		shading_flags.HS_SHADING_SHADE_MODE
					(n.i.)
	
GRvar		calibrate		GRvar.var_list.ivalue (n.i.)

