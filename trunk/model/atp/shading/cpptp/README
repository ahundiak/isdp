COMMAND
  Convert Parallel Projection to Perspective
  Convert Perspective Projection to Parallel

VERSION
  2.0.1.0

TEST PROCEDURE 

  Sm-shade iso window
  Shade front window rhl
  Convert top (keyin), iso (data-point) & front (keyin) windows to perspective
  Snapshot (db & si)
  Convert top (data-point), iso (keyin) & front (keyin) windows to parallel

SNAPSHOT

  IGEgragad	 projection information		gragad

