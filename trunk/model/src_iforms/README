03 Nov 2010 - CLIX Only Fixes

ATTENTION:

The files in this directory or any directories below are the the direct
result of I_forms fixes that could not be incorporated into an I_forms
build before the release of the EMS product.  In the case the I_forms is
rebuilt with the fixes included within, the appropriate files should be 
removed from that version of the EMS product dloadable directories.  Until
that time these files should be compiled for dload relocation in the 
$PRODUCT/config/dload/patch directory.  The make.sh in the $MODEL/src_xforms
directory is responsible for building these files.

Scott Walters  
03/21/97
