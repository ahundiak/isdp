#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main(int argc, char* argv[])
{
  char *buf,*p;
  FILE *file;

  printf("Entering %s\n",argv[0]);
  
  // file = fopen("main.c","rt");

  // fread(buf,250,1,file);

  p = strchr(buf,'i');
  
  // fclose(file);
}

