#include "VDppl.h"

extern VDdrw_setSumSortFlag();

main()
{
	IGRshort	retFlag;

	retFlag = VDdrw_setSumSortFlag();
	if( retFlag ) {
		UI_status("Table Summarization and Sort ON");
	}
	else {
		UI_status("Table Summarization and Sort OFF");
	}
}
