#include "VDppl.h"
#include "VDobj.h"

extern VDblobCreateBlobObject();
extern VDblobGetBlobObject();

extern VDblobLoadFromFile();
extern VDblobSaveToFile();

main()
{
  TGRid blobID;
  
  // VDblobCreateBlobObject(OM_K_NOT_AN_OS,"blob01",&blobID);
  VDblobGetBlobObject(OM_K_NOT_AN_OS,"blob01",&blobID);
  vdobj$Print(objID = &blobID);

  // VDblobLoadFromFile(&blobID,"3430_pcmk.xml");

  VDblobSaveToFile(&blobID,"3430_pcmk.xml1");
  
}
