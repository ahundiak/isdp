/* Do not alter this SPC information: $Revision: 1.1.54.1 $ */
/*
**      NAME:                                  sysalloc.prt
**      AUTHORS:                               Sunil Murthy
**      CREATION DATE:                         3/94
**      ABSTRACT:
**      Prototypes for system calls that are NOT to be called directly in
**      RIS source code other than in the common allocation routines.
**      Developers need to use the foll. macros defined in rismem.h
**      instead of the prototypes in this header.
**      SYS_MALLOC, SYS_REALLOC, SYS_CALLOC, and SYS_FREE.
**      Include comalloc.prt.
**
**      REVISION HISTORY:
*/

#if defined(WIN32)
#define RISEXPORT _CRTIMP
#else
#define RISEXPORT
#endif
 
extern RISEXPORT void *malloc(
	size_t)
;

/******************************************************************************/

extern RISEXPORT void *realloc(
	void *,
	size_t)
;

/******************************************************************************/

extern RISEXPORT void *calloc(
	size_t,
	size_t)
;

extern RISEXPORT void free(
	void *)
;

